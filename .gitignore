# See https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files for more about ignoring files.

# compiled output
dist
tmp
out-tsc

# dependencies
node_modules

# IDEs and editors
/.idea
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# IDE - VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# misc
/.sass-cache
/connect.lock
/coverage
/libpeerconnection.log
npm-debug.log
yarn-error.log
testem.log
/typings

# System Files
.DS_Store
Thumbs.db

.nx/cache
.nx/workspace-data

vite.config.*.timestamp*
vitest.config.*.timestamp*

# Systemd env files with secrets
**/.env.systemd
apps/api-gateway/firebase-key.pem

# Bot instance data and user data (changes rapidly, contains sensitive info)
data/bot-instances/
apps/bot-orchestrator/data/bot-instances/
**/freqtrade-instances/
**/bot-instances/

# Exclude all user token files
*_token*.txt
working_token*.txt
current_token.txt
fresh_token*.txt
latest_token*.txt

# Logs
logs/
*.log
.dev-logs/
.dev-pids/

# Temporary shell scripts (if any local test scripts are created)
# Production deployment scripts are committed, but local ones should be ignored
**/local-*.sh
**/temp-*.sh
**/test-*.sh

# Local development environment files (never commit)
**/.env
**/.env.local
**/.env.development.local
**/.env.test.local
**/.env.production.local

# System env files with secrets
**/.env.systemd
**/serviceAccountKey.json
